# Progress Tracker (VS Code)

Referensi progres sesi ada di:
- `docs/progress-board.json`

Command helper (CMD):

```cmd
scripts\check-progress.cmd show
```

Update status (CMD):

```cmd
scripts\check-progress.cmd update B12 on_progress "Update note"
```
